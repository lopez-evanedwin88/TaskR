import {StyleSheet} from 'react-native';

const styles = StyleSheet.create({
  media: {
    width: 100,
    height: 100,
    marginBottom: 5,
    alignSelf: 'flex-end',
  },
});

export default styles;
