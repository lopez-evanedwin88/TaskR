import {StyleSheet} from 'react-native';

const styles = StyleSheet.create({
  media: {
    width: 300,
    height: 200,
    marginBottom: 5,
    alignSelf: 'flex-end',
  },
});

export default styles;
