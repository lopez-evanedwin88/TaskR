export const authUser = async (params: any) => {
  return 0;
};

export const getUser = async (params: any) => {
    return 0;
  };

export const createTask = async (params: any) => {
  return 0;
};

export const getTasks = async (params: any) => {
  return 0;
};

export const updateTask = async (params: any) => {
    return 0;
  };

export const uploadMedia = async (params: any) => {
  return 0;
};
